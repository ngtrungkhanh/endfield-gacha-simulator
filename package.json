{
  "name": "arknights-endfield-gacha-simulator",
  "version": "1.1.0",
  "description": "Giả lập gacha Arknights: Endfield",
  "type": "module",
  "main": "js/app.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:offline": "node scripts/build-offline.mjs",
    "build:single": "node scripts/build-single-file.mjs",
    "package": "node scripts/package-offline.mjs",
    "watch": "esbuild js/app.js --bundle --outfile=js/bundle.js --format=iife --watch",
    "test": "node --test"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
